(DEFUN PLUSPLUS (X) (PLUS X 1))
(PLUSPLUS X 1)

(DEFUN TRIPLEADD (X Y Z) (PLUS X (PLUS Y Z)))
(TRIPLEADD X Y)

(DEFUN S1NOTLIST X (T))
(S1NOTLIST 2)

(DEFUN S2INVALIDFOREVAL () (T))
(S2INVALIDFOREVAL)

(DEFUN DEFUN () NIL)

(DEFUN XX ())

(DEFUN (PLUS 1 1) () ())